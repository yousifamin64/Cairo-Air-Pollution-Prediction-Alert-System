FROM python:3.11-slim

WORKDIR /app
COPY . .

RUN pip install --no-cache-dir --upgrade pip \
    && pip install --no-cache-dir -r requirements.txt

ENV PYTHONPATH=/app
EXPOSE 8501

CMD ["streamlit", "run", "src/gui/dashboard.py", "--server.port=8010", "--server.address=0.0.0.0"]
